# 오늘 뭐 먹지 고민 끝 — 프로젝트 구조 문서

가산디지털단지 사무실 주변 점심 메뉴를 자동으로 추천해주는 웹 도구. 본 문서는
**기획 의도 → 핵심 기능 → 시스템 구조 → 구현 상세(기술적 도전과 해결) → 파일/데이터 구성** 순으로
프로젝트 전체를 정리한다.

---

## 1. 기획 배경 및 목적

### 문제 정의
- 가산디지털단지처럼 반경 300m 안에도 식당이 수백 곳인 지역에서는, 오히려 "선택지가 너무 많아서"
  점심 메뉴를 정하기 어렵다 (선택의 역설). 매일 반복되는 이 작은 의사결정에 드는 피로를 줄이는 게
  목표다.
- 기존 지도 앱(카카오맵/네이버맵)은 "검색"은 잘 되지만 "그래서 오늘 뭐 먹을지 하나를 정해주는" 기능은
  없다. 조건(형태/종류/거리)만 고르면 그 조건에 맞는 후보 중 하나를 **즉시 무작위로 확정해주는**
  도구가 필요했다.
- 여러 명이 점심을 같이 먹을 때 "난 뭐든 좋아" "면은 빼고" 처럼 서로 다른 조건을 조율하는 것도
  피로하다 → 팀원 각자 조건만 입력하면 그 교집합에서 자동으로 하나를 뽑아주는 기능(팀 모드)까지 확장.

### 설계 철학: "식당"이 아니라 "메뉴" 단위 추천
초기 버전은 식당 단위로 추천했지만, 실제로 오늘 먹고 싶은 건 "식당"이 아니라 "메뉴"다(같은 식당이라도
어떤 날은 국밥이, 어떤 날은 비빔밥이 당길 수 있음). 그래서 추천의 최소 단위를 **(식당, 메뉴) 쌍**으로
재설계했다 — 형태(면/밥/국물…)는 메뉴 하나하나의 속성, 종류(한식/중식…에 대응하는 8분류)는 식당 단위
속성으로 분리해서 판정한다. (메뉴에 "맛(매운맛/순한맛/보통)" 태그와 필터도 있었으나, 사람마다 매운맛
기준이 달라 필터로서 의미가 없다는 피드백에 따라 2026-09-18에 제거함)

---

## 2. 핵심 기능

| 기능 | 설명 |
|---|---|
| 실시간 식당 검색 | 카카오맵 JS SDK로 사무실 주소 기준 반경 내 음식점을 실시간 조회 (하드코딩된 목록 아님) |
| 반경 선택 | 100~500m, 100m 단위로 즉시 재검색 |
| 철길 좌/우 자동 필터 | 1호선을 기준으로 사무실과 같은 쪽 식당만 후보로 삼음 (좌표 하드코딩 없이 실시간 계산) |
| 형태/종류 멀티 필터 | 면·밥·국물·구이·튀김·분식·기타 / 8종 점심 스타일 분류 |
| 메뉴·가격 자동 조회 | 카카오맵(1차)·네이버플레이스(보조)에서 실제 메뉴명/가격/사장님 추천 여부를 가져와 표시 |
| 운영시간 기반 제외 | 13시 이후에 문을 여는 저녁 전용 매장은 점심 후보에서 자동 제외 |
| 수동 편집/보정 | 자동 판정(종류/형태/포함여부/메뉴)이 틀리면 사용자가 직접 고쳐서 저장, 재검색해도 유지 |
| "이 가게 넘기기" | 메뉴가 아주 많은 프랜차이즈 매장만 계속 뽑히는 문제를 막기 위해 세션 한정으로 후보에서 제외 |
| 팀 모드 | 초대 코드로 방을 만들어 여러 명이 각자 조건(형태/종류/반경)을 제출하면, 교집합에서 공동 추천 결과를 뽑아 공유 |
| 지도 시각화 | 반경 원, 포함/제외 식당 마커, 추천 결과 하이라이트, 길찾기 딥링크 |
| 데모 폴백 모드 | 카카오 SDK 로드가 막힌 환경(Claude Artifact 등)에서는 내장 예시 데이터로 자동 전환 |

---

## 3. 시스템 아키텍처

빌드 도구나 프레임워크 없이, **정적 프론트엔드 1개 파일 + 서버리스 프록시 1개 파일**로 구성된
경량 구조다.

```
┌─────────────────────────────────────────────────────────────────┐
│  사용자 브라우저                                                  │
│                                                                   │
│  lunch-recommender.html  (GitHub Pages, 정적 호스팅, 빌드 없음)  │
│   ├─ Kakao Maps JS SDK ──────────────► 카카오 공식 Local API      │
│   │   (Geocoder / Places.categorySearch / keywordSearch)         │
│   │                                                               │
│   ├─ fetch() ──► kakao-menu-proxy (Cloudflare Worker)            │
│   │                ├─ 카카오맵 비공식 place-api(panel3)          │
│   │                │    └─ 메뉴/가격 + 운영시간(open_hours)      │
│   │                ├─ 네이버플레이스 SSR 페이지 파싱(보조 메뉴)  │
│   │                └─ Cloudflare KV (ROOMS) ── 팀 모드 방 상태   │
│   │                                                               │
│   └─ localStorage ── 사용자 편집분 / 메뉴·운영시간 캐시 / 설정   │
└─────────────────────────────────────────────────────────────────┘
```

- **프론트엔드**: `lunch-recommender.html` 한 파일에 HTML/CSS/JS가 모두 들어있다. 별도 빌드 과정이
  없어 어떤 정적 호스팅(현재는 GitHub Pages)에도 그대로 올리면 동작한다.
- **백엔드(프록시)**: `kakao-menu-proxy.worker.js`는 Cloudflare Worker로 배포되는 서버리스 함수.
  브라우저에서 직접 호출할 수 없는 비공식 API를 대신 호출해 CORS를 우회해주고, 팀 모드의 공유 상태를
  Cloudflare KV에 저장한다.
- **외부 의존성**: 카카오맵 JS SDK(공식, 지도/검색/지오코딩), 카카오맵 비공식 내부 API(메뉴·운영시간,
  문서화되어 있지 않음), 네이버플레이스 SSR 페이지(보조 메뉴 소스).
- **영속 데이터**: 별도 DB 없이 세 계층으로 나뉜다 — ① 카카오 라이브 검색 결과(항상 최신, 저장 안 함),
  ② Cloudflare 측 캐시(엣지 캐시 API + KV, 방/메뉴 프록시 응답 재사용), ③ 브라우저 `localStorage`
  (사용자 편집분, 메뉴/운영시간 캐시, 반경 등 개인 설정).

---

## 4. 파일 구성

```
vibecoding/
├── lunch-recommender.html     메인 앱 (단일 파일, ~2,000줄: HTML+CSS+JS)
├── kakao-menu-proxy.worker.js Cloudflare Worker (~360줄: 메뉴/운영시간 프록시 + 팀 모드 API)
├── README.md                  사용법/배포 가이드
├── HANDOFF.md                 작업 인수인계용 설계 결정 로그
├── UI-REDESIGN-SPEC.md        디자인 시스템(컬러/타이포/그리드) 명세
└── PROJECT-ARCHITECTURE.md    (본 문서) 구조 문서
```

---

## 5. 구현 상세 — 기술적 도전과 해결

공모전 심사 관점에서 가장 중요한 "어떤 문제를 어떻게 풀었는가"를 항목별로 정리한다.

### 5.1 카카오 Local API의 검색 결과 45건 캡 우회
**문제**: 카카오 `categorySearch`는 한 번의 검색(원 하나)당 최대 45건까지만 반환한다. 가산디지털단지는
워낙 밀집한 상권이라 반경 200m만 돼도 이 캡에 걸린다. 중심점 기준 동/서/남/북 5곳을 고정 오프셋으로
나눠 검색하는 방식(고정 그리드)을 처음 썼는데, REST API로 직접 대조 검증한 결과 반경 400m에서 44%,
500m에서 59%의 식당이 누락되는 것을 확인했다(각 셀도 다시 45건 캡에 걸렸기 때문).

**해결**: `deepCategorySearch()` — 검색 결과가 캡(45건)에 걸리면 그 원을 4등분해서 다시 검색하고, 각
하위 원이 또 캡에 걸리면 다시 4등분하는 방식으로 **밀집도에 따라 필요한 만큼만 재귀적으로 세분화**한다
(`CONFIG.SEARCH_MAX_DEPTH`, `CONFIG.SEARCH_MIN_SUBRADIUS_M`으로 한도 설정). 500m까지는 실측
그라운드트루스와 정확히 일치함을 확인했고, 그래도 한도에 걸린 채 캡이 남으면 `state.searchIncomplete`
플래그로 UI에 "누락 가능성" 경고를 띄운다.

### 5.2 카카오 API가 메뉴/가격을 제공하지 않는 문제
**문제**: 카카오 Local API(공식)는 식당명·카테고리·좌표만 줄 뿐 메뉴나 가격 정보는 제공하지 않는다.

**해결**: 카카오맵 웹이 내부적으로 쓰는 **비공식 API**(`place-api.map.kakao.com/places/panel3/{placeId}`)
가 메뉴 데이터를 갖고 있다는 것을 확인하고, 이를 호출하는 Cloudflare Worker 프록시
(`kakao-menu-proxy.worker.js`)를 만들었다. 이 API는 카카오 자체 도메인으로만 CORS가 열려 있어
브라우저에서 직접 호출이 불가능하기 때문에, Worker가 `Origin`/`Referer`/`User-Agent` 등을
`place.map.kakao.com`인 것처럼 설정해 대신 요청하고, 필요한 필드만 추려 CORS 허용 응답으로 돌려준다.
문서화되지 않은 API라 구조가 바뀔 수 있으므로, 실패 시 호출부(`fetchMenuFrom`)가 조용히 기존
키워드 추정(`TAG_RULES`) 방식으로 폴백하도록 설계했다.
추가로 **네이버플레이스**를 보조 메뉴 소스로 지원한다 — 네이버 지역검색 API는 place id를 안 주기 때문에
자동 매칭은 포기하고, 사용자가 직접 입력해둔 네이버 place id에 한해 네이버 페이지(SSR)의
`window.__APOLLO_STATE__`를 파싱해 동일한 스키마로 맞춰 반환한다(`handleNaverMenu`).

### 5.3 운영시간(13시 이후 오픈 상점) 필터
**문제**: 저녁·술자리 전용으로 늦게 여는 매장(예: 이자카야, 술집)이 점심 추천에 섞여 나오는 문제.

**해결**: 메뉴를 가져올 때 이미 호출하던 동일한 비공식 API 응답(`panel3`)에 오늘자 운영시간
(`open_hours`)이 함께 포함돼 있음을 확인하고, **별도 API 호출 없이** 같은 응답에서 오늘 오픈 시각을
분 단위로 추출해(`parseOpenHours`) 함께 반환하도록 확장했다. 프론트엔드는 오픈 시각이 13시(설정 가능,
`CONFIG.LUNCH_OPEN_CUTOFF_HOUR`) 이후인 식당을 반경/철길 조건과 동일한 방식으로 "제외됨" 처리한다
(`isExcludedByLunchHours`, `mergeRecord`). 운영시간 정보가 아직 도착하지 않았거나 조회에 실패한 경우엔
**제외하지 않는 것을 기본값**으로 삼아, 잘못된 제외로 인해 정상 식당이 안 보이는 사고를 방지했다.
반경 조건과 마찬가지로 자동 판정이 틀렸을 때는 사용자가 수동으로 "포함"으로 되돌릴 수 있다.

### 5.4 메뉴 태깅 정확도 — 캐시 무효화 설계
**문제**: 메뉴명 키워드로 형태(면/밥/국물…)를 추정하는 규칙(`TAG_RULES`, 37개)이 완벽하지 않다.
판정 로직을 수정해도 이미 24시간 TTL로 캐싱된 예전 판정 결과가 그대로 재사용되는 문제가 있었다.
(같은 문제가 네이버 메뉴 프록시 워커에서도 있었음 — 네이버가 메뉴 응답 스키마를 바꿔 파싱 로직을
고쳤는데도, 예전 로직으로 캐싱된 빈 응답이 계속 반환돼 재배포해도 안 고쳐진 것처럼 보였다. 2026-09-18
`NAVER_MENU_SCHEMA_VERSION`을 캐시 키에 추가해 동일한 방식으로 해결함 — `kakao-menu-proxy.worker.js`)

**해결**: 캐시 키에 `TAG_RULES_VERSION`(태그 규칙 버전)을 포함시켜, 판정 로직을 바꿀 때마다 이 버전을
올리면 예전 판정으로 캐싱된 항목이 자동으로 무효화되고 재조회·재태깅되도록 설계했다. 또한 리팩터
이전엔 캐시가 `{signatureMenu, otherMenu}` 객체 형태였는데, 새 코드가 기대하는 배열 형태가 아니면
캐시 미스로 취급해 조용한 폴백을 방지하는 방어 코드도 추가했다(`menuCacheGet`).

### 5.5 철길 좌/우 자동 판정 (좌표 하드코딩 없이)
**문제**: "1호선 서쪽만" 이라는 조건을 좌표로 하드코딩하면 사무실 위치가 바뀌거나 다른 지역에
적용하기 어렵다.

**해결**: `가산디지털단지역`·`독산역`(1호선) 두 지점을 실시간 키워드 검색으로 조회한 뒤, 사무실
좌표와 각 식당 좌표에 대해 **외적(cross product) 부호**를 계산해 어느 쪽에 있는지 자동 판정한다
(`crossSign`). 환승역이라 검색 결과에 여러 노선이 섞여 나올 수 있어, "1호선" 키워드가 포함된 결과를
우선 선택하는 보정 로직(`keywordFirst`)도 있다.

### 5.6 종류(cuisine) 분류 체계 재설계
**문제**: 초기엔 한식/중식/일식/양식처럼 국가 기준으로 분류했는데, 실제로 점심을 고를 때 궁금한 건
"국밥집인지 백반집인지 구내식당인지"라 국가 기준 분류가 실사용과 맞지 않는다는 피드백을 받았다.

**해결**: 점심 결정 스타일 기준 8분류로 재편했다 — **구내식당/한식뷔페 · 국밥/탕/찌개 · 제육/백반/정식 ·
중식 · 면류/분식 · 돈까스/일식/양식 · 아시안/세계요리 · 간편식/식단관리**. 카카오 `category_name` 기반
규칙을 최우선으로 적용하고, 못 찾으면 메뉴명 키워드로 폴백, 그래도 없으면 `제육/백반/정식`을 최종
기본값으로 둔다. 분류 체계가 바뀌기 전 저장된 사용자 편집값이 새 8분류에 없는 값이면 무시하고
자동으로 재추정하도록 방어 코드를 추가해, "필터의 어떤 칩을 골라도 안 걸리는" 조용한 버그를 막았다.

### 5.7 팀 모드의 동시 쓰기 경쟁 상태(race condition) 방지
**문제**: 여러 팀원이 거의 동시에 "준비" 버튼을 누르는 게 팀 모드의 핵심 사용 패턴인데, 방(room) 전체
상태를 하나의 KV 키로 관리하면 read-modify-write 방식이라 나중에 쓰는 사람이 앞사람의 변경사항을
통째로 덮어써서 유실시킬 수 있다(테스트 중 실제로 재현됨).

**해결**: 방 메타데이터(`room:{code}:meta`)와 멤버별 상태(`room:{code}:m:{memberId}`)를 **서로 다른
키로 분리**해서 저장한다. 각자 다른 키에 쓰는 구조라 이 경쟁 상태가 원천적으로 발생하지 않는다.
방은 Cloudflare KV에 6시간 TTL로 저장되며, 클라이언트는 4초 간격 폴링으로 최신 상태를 반영한다.

### 5.8 제한된 실행 환경(CSP) 대응 — 듀얼 모드
**문제**: Claude Artifact 미리보기처럼 CSP가 `dapi.kakao.com` 스크립트 로드를 막는 환경에서는
카카오 SDK 자체가 로드되지 않는다.

**해결**: SDK 스크립트의 `onerror`, 4초 로드 타임아웃, 로드 후 API 상태 체크를 모두 감시해 실패를
감지하면 **내장된 예시(seed) 데이터로 자동 폴백**한다(`getSeedRestaurants`). 사용자 개입 없이 파일
하나로 "정식 배포 환경 = 라이브 데이터 / 제약된 환경 = 데모 데이터" 듀얼 모드가 동작한다.

---

## 6. 데이터 흐름 요약

1. `init()` → 사무실 주소를 좌표로 변환(Geocoder) → 반경 내 음식점 검색(`deepCategorySearch`)
   → 철길 좌/우 판정 → **키워드 추정치로 화면을 즉시 채움**(빈 화면 방지)
2. 백그라운드로 `enrichAndRefresh()`가 실행되어, 각 식당의 실제 메뉴(`enrichMenus`)와 운영시간
   (`fetchOpenHoursFor`)을 동시성 제한을 두고 조회 → 도착하는 대로 카드/목록을 갱신
3. 사용자가 조건(형태/종류)을 선택하면 `candidatePairs()`가 **(식당,메뉴) 쌍** 후보를 계산 →
   "오늘 뭐 먹지?" 클릭 시 그 중 하나를 무작위로 확정(직전 추천과 중복 회피)
4. 자동 판정이 틀렸다고 판단되면 사용자가 목록 패널에서 직접 수정 → `localStorage`의 `overrides`에
   저장되어 다음 재검색에도 유지
5. (팀 모드) 방 생성/참여 → 멤버별로 조건 제출 → 호스트가 "결과 뽑기" → 서버가 저장한 결과를 모든
   멤버가 폴링으로 동시에 확인

---

## 7. 기술 스택

| 구분 | 사용 기술 |
|---|---|
| 프론트엔드 | 순수 HTML/CSS/JavaScript (ES5 스타일, 빌드 도구·프레임워크 없음) |
| 지도/검색 | Kakao Maps JavaScript SDK (Geocoder, Places) |
| 메뉴/운영시간 데이터 | 카카오맵 비공식 내부 API(panel3), 네이버플레이스 SSR 파싱(보조) |
| 백엔드(프록시) | Cloudflare Workers (서버리스, 빌드 없이 단일 JS 파일 배포) |
| 저장소 | Cloudflare Cache API(엣지 응답 캐시) · Cloudflare KV(팀 모드 방 상태) · 브라우저 `localStorage`(사용자 데이터) |
| 배포 | GitHub Pages(프론트엔드, 정적 호스팅) + Cloudflare Workers(프록시) |

---

## 8. 한계 및 향후 계획

- 비공식 API(카카오 panel3, 네이버 SSR)에 의존하므로 응답 구조가 예고 없이 바뀌면 메뉴/운영시간
  조회가 실패할 수 있다 — 실패 시 항상 기존 방식(키워드 추정)으로 조용히 폴백하도록 설계했지만,
  근본적으로 문서화되지 않은 API에 의존하는 구조적 한계는 남아있다.
- 폐업/변경 정보가 실시간 반영되지 않을 수 있어 "지도에서 보기" 링크로 최종 확인을 권장한다.
- 현재는 가산디지털단지·1호선 기준으로 사무실 위치/철길이 설정값에 있지만, 다른 지역/노선으로
  일반화하려면 설정 항목을 더 유연하게 다듬을 여지가 있다.
- 팀 모드는 현재 형태/종류/반경만 취합하며, 운영시간 제외 조건은 아직 팀 모드 API에 반영돼
  있지 않다 — 개인 모드와 동일하게 확장할 수 있는 다음 과제.
